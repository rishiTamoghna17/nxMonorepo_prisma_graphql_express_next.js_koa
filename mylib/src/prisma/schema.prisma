// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://postgres:frir7@aMX4Nc9@h@db.hvlovpmiicdsonejhahh.supabase.co:5432/postgres"
}
//"postgres://prisma_graphql_user:HCKRUG4P96S5AihAxvaPEUpaLmxNJ72e@dpg-chng9u7dvk4n43c52hbg-a.singapore-postgres.render.com/prisma_graphql"
model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String?
  password  String
  role      Role     @default(user)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Role {
  user
  admin
}

model Task {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  title  String?
  isComplete Boolean  @default(false)
}